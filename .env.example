# Copy to .env and adjust per environment.
# Base URL of the Django backend API. Must be NEXT_PUBLIC_-prefixed because it
# is used in browser-side fetch (lib/api.ts).
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000/api
